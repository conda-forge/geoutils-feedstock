{% set python_min = '3.10' %}
{% set name = "geoutils" %}
{% set version = "0.1.14" %}

{% set github_url = "https://github.com/GlacioHack/geoutils" %}
#{% set pypi_url = "https://pypi.io/packages/source/" %}

package:
  name: {{ name|lower }}
  version: {{ version }}
source:
   # url: {{ pypi_url }}/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  url: {{ github_url }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: bb06d3589e85f18614a9e6366d6516b9834051c9f42ea6decc7cc7b8acb7d44e

build:
  number: 2
  # To circumvent version generated by setuptools_scm
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python


requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
    - setuptools_scm
    - wheel
  run:
    - pip
    - python >={{ python_min }},<3.13
    - geopandas >=0.12.0
    - matplotlib-base =3.*
    - pyproj =3.*
    - rasterio >=1.3,<2
    - pandas >=1,<3
    - numpy =1.*
    - scipy =1.*
    - tqdm
    - xarray
    - dask
    - rioxarray =0.*

test:
  requires:
    - python {{ python_min }}
  imports:
    - geoutils

about:
  home: {{ github_url }}
  license: Apache-2.0
  license_file: LICENSE
  summary: Analysis of georeferenced rasters, vectors and point clouds

extra:
  recipe-maintainers:
    - rhugonnet
    - erikmannerfelt
    - adehecq
    - atedstone
