{% set pypi_url = "https://pypi.io/packages/source/" %}
{% set python_min = "3.10" %}
{% set name = "geoutils" %}
{% set version = "0.2.1" %}

{% set github_url = "https://github.com/GlacioHack/geoutils" %}
#{% set pypi_url = "https://pypi.io/packages/source/" %}

package:
  name: {{ name|lower }}
  version: {{ version }}
source:
   # url: {{ pypi_url }}/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  url: {{ github_url }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 049570ee97975ce9b06658d35d248003bd77613f3c765d5f6f2ee083904105c1

build:
  number: 0
  # To circumvent version generated by setuptools_scm
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python


requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools >=64
    - setuptools_scm >=8,<9.0
    - wheel
  run:
    - pip
    - python >={{ python_min }},<3.14
    - geopandas >=0.12.0
    - matplotlib-base =3.*
    - pyproj =3.*
    - rasterio >=1.3,<1.4.4
    - pandas >=1,<3
    - numpy >=1,<3
    - scipy =1.*
    - tqdm
    - xarray >2023
    - dask
    - rioxarray =0.*
    - affine
    - shapely

test:
  requires:
    - python {{ python_min }}
  imports:
    - geoutils

about:
  home: {{ github_url }}
  license: Apache-2.0
  license_file: LICENSE
  summary: Analysis of georeferenced rasters, vectors and point clouds

extra:
  recipe-maintainers:
    - rhugonnet
    - erikmannerfelt
    - adehecq
    - atedstone
